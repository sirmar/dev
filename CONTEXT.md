# dev — Domain Context

## What this is

`dev` is a language-agnostic developer lifecycle CLI built on Docker. It provides a uniform command surface (`build`, `lint`, `check`, `ci`, `run`, …) regardless of language or toolchain — all language-specific logic lives inside Dockerfile stages and Compose files, never in the shell scripts.

`mdev` is a monorepo orchestration layer that discovers services (sub-directories containing a `.dev` file) and fans out `dev` commands across them.

## Core concepts

### `.dev` config file

A shell script sourced at startup. Declares the project identity and behaviour:

- `DEV_NAME` — image and container name prefix (required)
- `DEV_REPO_TYPE` — one of `tool`, `service`, `library`, `e2e`, `image` (required)
- `DEV_CONTEXT` — Docker build context directory (default `.`)
- `DEV_PORT`, `DEV_NETWORK`, `DEV_MOUNTS`, `DEV_SCRIPTS`, `DEV_DB_*` — optional feature toggles

`find_dev_file` is the single canonical reader — it walks up the directory tree and sets `ROOT_DIR`. All config access goes through this function; nothing reads `.dev` with grep or sed.

### `.mdev` config file

Sources `MDEV_NAME` and an optional `MDEV_SERVICES` comma-separated list. If `MDEV_SERVICES` is absent, services are auto-discovered by finding all `.dev` files one level deep under the monorepo root.

### Repo types

| Type | Meaning |
|------|---------|
| `tool` | CLI tool; has a `prod` Dockerfile stage, supports `run` |
| `service` | Long-running server; has Compose, supports `up/down/shell/watch` |
| `library` | No prod image; lint/test only |
| `e2e` | End-to-end test suite; `run` drives `docker-compose.yml` |
| `image` | Base Docker image; `build` iterates all named stages |

### Command registry

`_DEV_COMMANDS` (array in `dev.sh`) is the single authoritative list of all `dev` subcommands. The repo-type permission matrix lives in `cmd_repo_types`, and static argument lists live in `cmd_args`. Both the runtime dispatcher and the shell completions derive from this registry — there is no second copy.

Similarly, `MDEV_COMMANDS` + `cmd_arg_type` in `mdev.sh` serve the same role for `mdev`.

### Stages

A *stage* is a named `FROM … AS <name>` target in the Dockerfile. The canonical stages are: `base`, `lint`, `format`, `unit`, `types`, `security`, `coverage`, `lock`, `watch`, `prod`, `e2e`, `scripts`. `run_stage` is the universal helper — it checks for stage existence, builds the image quietly, then delegates to `run_in`. Commands like `cmd_lint`, `cmd_unit`, `cmd_watch` are thin wrappers around `run_stage`.

For stages that run inside a Docker Compose environment, `run_stage_compose` is the counterpart: it tears down before building, creates the `out` directory, and delegates to `run_compose_suite`. `cmd_coverage` and `cmd_e2e` use it. The `compose_mode` parameter (`required`/`optional`) controls whether a missing compose file is a skip or a fallback to direct Docker.

### Completions

`completions/dev.bash` and `completions/_dev` (bash and zsh) are generated by `src/generate-completions.sh` — do not edit by hand. The script sources `dev.sh` directly so it reads `_DEV_COMMANDS`, `cmd_repo_types`, and `cmd_args` without duplication. The same applies to `mdev.bash` / `_mdev`.

### Introspection commands

Two hidden subcommands support tooling and shell completions without coupling to internal contracts:

- `dev supports <cmd>` — exits 0 if `<cmd>` is available for the current repo type, 1 otherwise. Used by `mdev` to decide whether to delegate a command to a service.
- `dev list-scripts` — prints `DEV_SCRIPTS` entry names one per line. Used by shell completions to offer `exec` argument candidates.

### Dev result format

Every check command writes a result file to `out/` after each run. Three schemas:

**Node-ID results** (`unit`, `e2e`):
```json
{ "passed": true, "failures": [{ "node_id": "<runner-native id>" }] }
```
`node_id` is opaque — runner-native (e.g. `src/tests/unit/test_auth.py::test_login` for pytest). Agents pass it back verbatim; they never construct or parse it. Written by the image entrypoint via a normalizer script (`unit-normalizer`), not by `dev.sh`.

**Simple results** (`lint`, `lint-dockerfile`, `types`, `security`, `coverage`):
```json
{ "passed": true, "failures": [] }
```
Written by `_write_simple_result` in `dev.sh` based on Docker exit code. `failures` is always empty but present for schema consistency.

**Check pipeline** (`check`): `out/check-result.json` with a `stages` array — see `docs/dev-commands.md`.

When `CLAUDECODE=1`:
- `unit`/`e2e`: `failures[]` non-empty → run scoped to those `node_id` values (narrowing); full suite on scope reset
- `check`: stages that passed are skipped; full pipeline on scope reset
- All: result JSON re-emitted as the final stdout line

`mdev` aggregates per-service result files into a workspace-level result. For `unit`/`e2e` the workspace result includes a `services` array with per-service failures. For simple commands the `services` array tracks per-service pass/fail for narrowing. Under `CLAUDECODE=1`, services that passed are skipped (service-level narrowing).

## Key invariants

- No language/toolchain logic in `dev.sh` or `mdev.sh` — it belongs in Dockerfiles.
- All `.dev` parsing goes through `find_dev_file` / `load_config` — no ad-hoc grep/sed elsewhere. `mdev` reads individual service `.dev` files via subshell sourcing in `read_dev_var`, not grep.
- The command list and repo-type matrix are defined once in the registry functions and read everywhere else.
- Completions are always regenerated via `src/generate-completions.sh`; the output files are committed.
